# دليل العمل اليومي بالفريق باستخدام Git و GitHub

---

## ✅ القسم الأول: الروتين اليومي السريع (اللي تطبّقونه كل يوم)

### ١) قبل ما تبدأ تشتغل

كل مرة تفتح اللابتوب وتبغى تشتغل على المشروع:

```bash
cd C:\xampp\htdocs\find_the_five

git checkout main
git pull origin main
```

* هذا يضمن إنك على آخر نسخة من الكود في الفرع `main`.

لو عندك **مهمّة جديدة** (أول مرة تشتغل عليها):

```bash
git checkout -b feature/اسم_الميزة
```

أمثلة لأسامي فروع:

```bash
git checkout -b feature/auth
git checkout -b feature/admin-panel
git checkout -b feature/dashboard
git checkout -b feature/labs
```

لو كنت عامل الفرع من قبل وتبغى **تكمل عليه**:

```bash
git checkout feature/اسم_الميزة
git pull origin feature/اسم_الميزة
```

> **قاعدة ذهبية:**
> ما حد يشتغل مباشرة على `main`
> كل واحد يشتغل على فرع `feature/...`

---

### ٢) أثناء الشغل

* عدّل في الملفات اللي تخص مهمتك فقط.
* جرّب الكود على:

  * `http://localhost/find_the_five/`

ما في أوامر Git هنا، بس كود وتجارب.

---

### ٣) بعد ما تخلص جزء منطقي من الشغل

كل ما تخلص خطوة تستاهل تنحفظ (مو كل تعديل صغير):

```bash
git status          # يشوف إيش تعدل
git add .           # يضيف كل التعديلات
git commit -m "شرح مختصر لما سويت"
git push origin feature/اسم_الميزة
```

بعدها:

1. افتح GitHub
2. افتح Pull Request من `feature/اسم_الميزة` → `main`
3. انتظر صاحب المشروع يراجع ويمرج.

---

### ٤) اليوم اللي بعده / قبل أي شغل جديد

قبل أي شغل جديد، لازم:

```bash
git checkout main
git pull origin main
```

بعدها:

* ترجع لفرعك القديم:

  ```bash
  git checkout feature/اسم_الميزة
  ```
* أو تنشئ فرع جديد لمهمة جديدة:

  ```bash
  git checkout -b feature/اسم_الميزة_الجديدة
  ```

---

## 🧠 القسم الثاني: شرح أوامر Git المستخدمة (ببساطة)

### `git status`

يعرض حالة الملفات:

* ملفات جديدة
* ملفات معدّلة
* ملفات جاهزة للـ commit

---

### `git checkout main`

ينقلك للفرع `main` (النسخة الرسمية المستقرة من المشروع).

---

### `git pull origin main`

يسحب آخر نسخة من الكود في `main` من GitHub إلى جهازك.

---

### `git checkout -b feature/اسم_الميزة`

ينشئ فرع جديد من `main` باسم:
`feature/اسم_الميزة`
وينقلك عليه مباشرة.

مثال:

```bash
git checkout -b feature/auth
```

---

### `git checkout feature/اسم_الميزة`

ينقلك لفرع موجود من قبل عشان تكمل الشغل فيه.

---

### `git add .`

يضيف كل الملفات المعدّلة عشان تدخل في commit القادم.

---

### `git commit -m "رسالة"`

يحفظ لقطة من التعديلات اللي سويتها مع رسالة توضّح:

```bash
git commit -m "Implement login and register"
git commit -m "Connect dashboard to achievements table"
```

---

### `git push origin feature/اسم_الميزة`

يرفع شغلك على الفرع إلى GitHub.

---

## ⚠️ القسم الثالث: قوانين الفريق

1. **ممنوع التعديل أو الـ commit مباشرة على `main`.**
   دائمًا اشتغلوا على فروع: `feature/...`.

2. **قبل أي شغل جديد:**

   ```bash
   git checkout main
   git pull origin main
   ```

3. **كل مهمة = فرع:**

   * `feature/auth`
   * `feature/dashboard`
   * `feature/admin`
   * `feature/labs`

4. **بعد ما تخلص شغلك:**

   * `git add .`
   * `git commit -m "..."`
   * `git push origin feature/...`
   * افتح Pull Request
   * صاحب المشروع هو اللي يراجع ويمرج على `main`.

5. لو خبّصت التعديلات وتبغى ترجع لآخر نسخة من الكود (محليًا) قبل آخر commit:

   ```bash
   git restore .
   ```

   > ⚠️ هذا يرجّع كل الملفات لحالة آخر commit،
   > يعني أي شغل جديد ما عملت له commit → يطير.

---

بهذا الشكل يمديك تحط الملف في الريبو، وتقول للفريق:

> اقرأوا هذا القسم فوق كل يوم
> والباقي تحت لو حابين تفهموا الأوامر أكثر.

```
::contentReference[oaicite:0]{index=0}
```
